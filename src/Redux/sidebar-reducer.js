let initialState = {
    friends: [
        {id: 1, name: 'Ksenya'},
        {id: 2, name: 'Dasha'},
        {id: 3, name: 'Volodya'}
    ]
}

const sidebarReducer = (state = initialState, action) =>{
    return state
}

export default sidebarReducer;