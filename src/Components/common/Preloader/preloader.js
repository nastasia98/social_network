import Spinner from "../../../assets/images/Spinner.svg";
import React from "react";

let Preloader = (props) => {
    return <div>
        <img src={Spinner}/>
    </div>
}

export default Preloader;